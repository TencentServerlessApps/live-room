'use strict';

/*
Please use each package directly, for example, to use errors:
    const errors = require('js-kernel/errors');
    console.log(errors.data());

First of all, please install core package by:
    npm install ../kernel
 */
module.exports = {};

